language: ruby
rvm:
  - 2.4
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: master
  local_dir: _site
  repo: molonc/aparicio_website_src
  target_branch: aparicio
  fqdn:
notifications:
  email:
    recipients:
    on_success: always
    on_failure: always
